# Arise Frontend

React + Vite frontend for the Arise market dashboard.

## Run

```bash
cd Frontend
npm install
copy .env.example .env
npm run dev
```

The frontend expects the FastAPI backend to be running on `http://127.0.0.1:8000`.
